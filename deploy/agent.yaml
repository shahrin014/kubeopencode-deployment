apiVersion: kubeopencode.io/v1alpha1
kind: Agent
metadata:
  name: opencode-agent
spec:
  config: /tools/opencode.json
  credentials:
    - name: opencode
      secretRef:
        name: opencode-credentials
  workspaceDir: /workspace
  serviceAccountName: opencode-agent